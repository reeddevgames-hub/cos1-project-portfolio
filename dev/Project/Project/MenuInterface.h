#pragma once
class MenuInterface
{

public:
	enum MenuOption

		// This makes The options that will be in the menu an enum which can be easier to call later.
	{
		CreateAccount,
		Deposit,
		Withdraw,
		Transfer,
		Exit
	};


	

};

